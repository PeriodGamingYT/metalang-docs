/ You don't have to write all that boilerplate though, that is taken care of automatically.
  It behaves the exactly the same way as the "full-boilerplate.meta" code. /
log("Hello, World!") @clear-screen, @new-line / These flags basically add stuff to the "log" function. /